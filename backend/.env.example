# API Key for authentication
# Generate a secure random key for production
API_KEY=your-api-key-here

# CORS Configuration (comma-separated list of allowed origins)
# Add any additional origins you need to access the API from
# Include your production domain with https://
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8000,https://your-production-domain.com
